# Deploy-Key-Paar NIE committen (privater Schluessel!)
grafana_changelog_deploy
grafana_changelog_deploy.pub
*.pem

# Lokale Filter-Blacklist + gefilterte Ausgaben (bleiben lokal)
blacklist.txt
filtered/
